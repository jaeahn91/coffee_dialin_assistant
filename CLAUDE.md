# CLAUDE.md — coffee dial-in assistant

AI 커피 다이얼인 어시스턴트. 로스터가 발급한 QR로 손님이 추출 결과(맛/강도 등)를 입력하면,
**규칙 기반**으로 다음 레시피 조정 처방을 한 줄로 돌려준다. 설계 출처는 `docs/PRD_*_v0.3.md`.

## 새 세션·compact 시 먼저 할 일
- **`docs/NEXT.md`를 먼저 읽는다** — 지금 할 일과 현재 상태 스냅샷이 거기 있다.
- 미결정/확정 결정을 존중한다. 예: **§5 "괜찮아요" 경로 = C(경량)** — 시간필터·온도트리 건너뛰고 맛+강도 2질문 → 격자.
- compact할 때 보존 우선순위: 진행 중 작업의 다음 스텝, 확정된 설계 결정, 미해결 미결정 항목.

## 아키텍처 계층 (경계를 지킬 것)
- `lib/domain/` — **순수 규칙 엔진. DB/AI/프레임워크 비의존.** 격자(§6-1)·온도트리(§6-2)·거울상 불변식(§6-3)·고정 템플릿 출력(§9). 외부 호출 금지.
- `lib/config/` — 튜닝 노브(조정 폭 등 부록5 잠정값). 값만 바꿔 처방 강도 조정.
- `lib/db/` — (예정) 테넌트 격리 데이터 접근. `roaster_id` 기준 RLS. *주의: 레포의 `data/`는 추출 로그 원본 데이터셋이라 이름 충돌 회피용으로 `lib/db/` 사용.*
- `app/` — Next.js 16 App Router UI. 도메인 엔진을 호출만 한다(로직 중복 금지).

## 불변식 (깨지면 안 됨)
- 모든 **방향성** 처방(분쇄 곱게/굵게, 물 늘림/줄임, 온도 up/down)은 시스템 어딘가에 역방향 경로가 있어야 한다(§6-3). 온도 `reset`만 예외.
- §9: 처방 설명은 **고정 템플릿**. 생성형 텍스트 아님.
- 이 불변식은 `lib/domain/invariants.test.ts`로 자동 검증된다 — **항상 green 유지**.

## 명령
- `npm run dev` — 개발 서버
- `npm run check` — **타입체크 + lint + 테스트 (커밋 전 자동 실행: husky pre-commit)**
- `npm test` — 테스트만

## 협업 규칙
- 비자명한 작업은 **구현 전 합의** 후 진행.
- 커밋은 자유(로컬·되돌리기 쉬움), **push는 매번 확인** 후.
- 비밀값은 `.env.local`(깃 제외)에만. 키 값을 채팅/커밋에 노출하지 않는다. 검증은 형식만.
