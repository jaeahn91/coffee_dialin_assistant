# 다음 작업 (NEXT)

> 세션이 끊겨도 여기서 바로 이어받기 위한 메모. 끝난 항목은 지우거나 체크.

## 지금 할 일 — §5 플로우 상태머신 + 사용자 UI
- **목표**: 도메인 룰 엔진(`lib/domain`)을 실제 사용자 흐름에 연결.
- **흐름**: 레시피 표시 → 만족도 3버튼(좋아요/괜찮아요/아쉬워요) → 질문 흐름 → 처방 한 줄(`describePrescription`).
- **만족도 라우팅 (PRD §5)**:
  - 좋아요(good) → `hold` (그대로 유지)
  - **괜찮아요(okay) → C(경량): 시간필터·온도트리 건너뛰고 맛+강도 2질문 → 격자(`gridCell`)** ✅ 확정
  - 아쉬워요(bad) → 풀 플로우(시간 편차 → 온도트리 / 격자)
- **범위**: 최소 형태부터. 디자인(Tailwind 꾸미기)은 나중. DB 저장 없이 먼저 인터랙션만.
- 상태머신은 순수 함수로 빼서 테스트 가능하게(룰 엔진과 같은 결).

## 그 다음 (대기)
- 구조 강화: (1) 40% 자동 compact 워크플로우, (2) 프로젝트 하네스(테스트·타입체크·lint·pre-commit 가드) — *이번 세션에서 진행 중*
- `lib/db` 데이터 접근 계층 + Supabase 9엔티티 스키마 + RLS (5단계, 키는 `.env.local`에 준비됨)
- 챗봇 폴백 스텁(§9 경계 — 룰은 AI 호출 안 함)

## 현재 상태 스냅샷 (2026-06-26)
- 도메인 룰 엔진 완료·커밋됨 (`c4bf0ae`): grid/tree/invariants/describe + vitest 5 pass.
- Next.js 16 스캐폴딩 완료. Node 22 LTS.
- Supabase 프로젝트 생성됨, 신형 키(`sb_publishable_`/`sb_secret_`) `.env.local`에 입력·검증 완료. 깃 추적 제외 확인.
- 미커밋: 없음 (작업 트리 clean). 아직 origin에 push 안 함(push 전 확인).
